#include <stdio.h>
/*
 * Write alphabetic order
 *
 * a-z in lower case
 *
 * return - always (0) success
 *
 */

int main (void)


{
	/*local variable defination*/
	int n; 

	for(n=a);

	while (a<z) {
		ch++,
		putchar(ch);
	}

	return (0);

}
